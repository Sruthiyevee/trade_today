# ============================================================
# .env.example — Copy this to .env and fill in your real keys
# NEVER commit the actual .env file to version control!
# ============================================================

# Groq (LLM for agents)
GROQ_API_KEY=your_groq_api_key_here

# Google Gemini (GenAI integration)
GOOGLE_API_KEY=your_google_api_key_here

# Langfuse (optional — only needed for LLM observability/tracing dashboards)
# Leave blank to disable telemetry; the app will still run without these.
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=https://cloud.langfuse.com

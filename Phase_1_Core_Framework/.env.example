# === FREE TIER API KEYS / MCP CONFIGURATIONS ===
# Do NOT commit the actual .env file, use this as a reference

# Groq Free Tier (Llama 3 inference for reasoning)
GROQ_API_KEY="your_groq_api_key_here"

# Google Gemini API (Free Tier for RAG / Document parsing)
GEMINI_API_KEY="your_gemini_api_key_here"

# Langfuse Observability Keys (Self-Hosted default local ports)
LANGFUSE_PUBLIC_KEY="pk-lf-..."
LANGFUSE_SECRET_KEY="sk-lf-..."
LANGFUSE_HOST="http://localhost:3000"

# Broker API configurations (Dhan / Shoonya) will go here later
BROKER_ID="example_id"
BROKER_SECRET="example_secret"
